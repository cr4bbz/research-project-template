# Analysis and visualisation

Put deterministic, reviewable computation here. A script that generates a paper figure
must document its inputs, output path, parameters, and the claim ledger entry it supports.

Do not hand-edit files in `paper/figures/generated/`; regenerate them instead.
