# Lean module

This is a standalone Lake project. From this directory run:

```text
lake build
```

Keep semantic definitions and theorems in named modules. Link paper-facing results in
`../../docs/CLAIM_LEDGER.md`; do not rely on source-file order as documentation.
